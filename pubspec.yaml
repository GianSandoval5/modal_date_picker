name: modal_date_picker
description: modal_date_picker is a flutter package that allows you to pick a date from a modal in TextFormFields.
version: 0.0.8
homepage: https://github.com/GianSandoval5/modal_date_picker

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

license: MIT
